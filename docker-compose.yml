services:
  web:
    build: .
    container_name: jellyfin-visualizer
    ports:
      - "8081:8080"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
